apply from: "$rootDir/gradle/spring-boot.gradle"

dependencies {
    runtime(
//            project(':modules:app-web'),
            project(':modules:app-autoui'),
            project(':modules:app-sample'),
    )
}
