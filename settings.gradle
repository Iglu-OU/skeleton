rootProject.name = new File(rootDir, 'etc/project-name').text

new File(rootDir, 'app').eachDir {
    include "app:${it.name}"
}

new File(rootDir, 'libs').eachDir {
    include "libs:${it.name}"
}

new File(rootDir, 'tools').eachDir {
    include "tools:${it.name}"
}
